{
  "name": "builder",
  "version": "1.0.0",
  "description": "This package contains custom builder for plugin",
  "builders": "builders.json",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm i && tsc"
  },
  "author": "Harisudarsan",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.3.3"
  }
}
